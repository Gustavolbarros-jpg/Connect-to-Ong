{
  "name": "project-root",
  "version": "1.0.0",
  "description": "Orquestrador do projeto Connect-to-Ong",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"bgBlue,bgCyan\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "test": "concurrently -n \"FRONT,BACK\" -c \"bgGreen,bgMagenta\" \"npm --prefix frontend test -- --watchAll=false\" \"npm --prefix backend test\"",
    "test:front": "npm --prefix frontend test -- --watchAll=false",
    "test:back": "npm --prefix backend test",
    "test:e2e": "npm run cy:run --prefix frontend"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
